<%= simple_form_for [@restaurant, @review] do |f| %>
<%= f.input :rating, as: :integer, label: "Rating", hint: "Between 0 and 5" %>
<%= f.input :content, as: :text, label: "Comment" %>
<%= f.button :submit, class: 'btn, btn-primary' %>
<% end %>